# Backend API origin.
# Leave unset in dev — calls to /api are proxied to http://localhost:8000 (see vite.config.ts).
# For a deployed build, point this at your API, e.g. https://scout-api.example.com
# VITE_API_BASE=https://your-api-host
